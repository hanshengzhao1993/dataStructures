// Divide two integers without using multiplication, division and mod operator.


var divide = var divide = function(dividend, divisor) {
  if (divisor === 0) {
    return Number.MAX_VALUE;
  }
  
  if (dividend === 0) {
    return 0;
  }

  
}


console.log( divide(7,2) );